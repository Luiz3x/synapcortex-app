# requirements.txt (v3.0 - Production Core)

# --- Núcleo da Aplicação Flask ---
Flask==2.3.3
python-dotenv==1.0.1
gunicorn==21.2.0

# --- Extensões do Flask ---
Flask-SQLAlchemy==3.1.1
Flask-Bcrypt==1.0.1
Flask-Cors==4.0.0
Flask-Migrate==4.0.5
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-SocketIO==5.3.6

# --- Framework da API de Alta Performance ---
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.5.3
pydantic-settings==2.1.0

# --- Conexão com Banco de Dados e Cache ---
psycopg2-binary==2.9.9
redis==5.0.1

# --- Segurança e Autenticação Moderna (Plano de Evolução) ---
PyJWT==2.8.0                 # Para geração e validação de JSON Web Tokens
python-jose[cryptography]==3.3.0 # Alternativa robusta para JWT com mais algoritmos

# --- Outras dependências ---
Jinja2==3.1.2
requests==2.31.0
alembic==1.13.1  # Dependência do Flask-Migrate (versão ajustada)
celery==5.3.6

# --- Instala o nosso próprio pacote synapcortex em modo editável ---
-e .